# requirements/dev.txt
# ============================================================
# Development-only dependencies (testing, linting, type checking)
# Install via: uv sync --extra dev
# ============================================================

# Includes all base requirements
-r base.txt

# Testing
pytest>=8.2
pytest-cov>=5.0
